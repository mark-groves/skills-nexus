{
  "skill_name": "pr",
  "trigger_evals": [
    {
      "id": 1,
      "query": "open a pull request for my branch",
      "should_trigger": true
    },
    {
      "id": 2,
      "query": "publish this branch as a GitHub PR against main",
      "should_trigger": true
    },
    {
      "id": 3,
      "query": "commit the staged files",
      "should_trigger": false
    },
    {
      "id": 4,
      "query": "review the code changes in this PR",
      "should_trigger": false
    },
    {
      "id": 5,
      "query": "open an Azure Repos pull request for this branch",
      "should_trigger": true
    },
    {
      "id": 6,
      "query": "draft the PR title and description but don't publish it",
      "should_trigger": true
    },
    {
      "id": 7,
      "query": "push this branch without creating a pull request",
      "should_trigger": false
    }
  ],
  "behavior_evals": [
    {
      "id": 1,
      "prompt": "/pr",
      "expected_behavior": "Resolves the repository default branch, drafts structured PR content, and publishes without a separate content-review gate when prerequisites pass.",
      "fixtures": [],
      "checks": [
        "Resolves the default branch from origin/HEAD or gh repo view when no argument is provided",
        "Stops if default branch resolution fails",
        "Does not assume main when the default branch cannot be determined",
        "Drafts a PR title under 70 characters",
        "Drafts a PR body with Summary and Validation sections",
        "Does NOT ask for approval of the title, body, or push command"
      ]
    },
    {
      "id": 2,
      "prompt": "Draft the PR title and description against release/2026-04, but do not authenticate, push, or publish.",
      "expected_behavior": "Uses the supplied target branch and its remote-tracking ref for a local-only draft instead of replacing it with the repository default.",
      "fixtures": [
        "explicit-release-base"
      ],
      "checks": [
        "Uses release/2026-04 as the base branch",
        "Inspects commits and diff against origin/release/2026-04 rather than silently replacing it with the default branch",
        "Does NOT authenticate, push, or create a pull request",
        "Returns a structured title and body based only on changes after release/2026-04"
      ]
    },
    {
      "id": 3,
      "prompt": "create a PR",
      "expected_behavior": "Stops when the working tree is dirty and tells the user to commit first unless they explicitly override.",
      "fixtures": [],
      "checks": [
        "Warns when uncommitted changes are present",
        "Suggests running /commit first",
        "Does not push or create a PR from a dirty tree unless the user explicitly overrides the dirty-tree safety stop"
      ]
    },
    {
      "id": 4,
      "prompt": "open a pull request",
      "expected_behavior": "Stops before drafting or publishing when the repository is in detached HEAD state.",
      "fixtures": [],
      "checks": [
        "Runs git symbolic-ref --quiet --short HEAD",
        "Detects detached HEAD when the command fails",
        "Tells the user to switch to a branch first",
        "Does not push or create a PR"
      ]
    },
    {
      "id": 5,
      "prompt": "publish this branch as a PR",
      "expected_behavior": "Stops when the current branch is the resolved default branch.",
      "fixtures": [],
      "checks": [
        "Resolves the repository default branch",
        "Blocks main and master",
        "Blocks the resolved default branch even when it is not main or master",
        "Does not push or create a PR from the default branch"
      ]
    },
    {
      "id": 6,
      "prompt": "/pr",
      "expected_behavior": "Pushes a branch with no upstream using a derived publish remote without a separate review gate.",
      "fixtures": [],
      "checks": [
        "Checks upstream tracking with git rev-parse --abbrev-ref @{upstream}",
        "Treats upstream lookup failure as no upstream",
        "Derives the publish remote from pushRemote, remote.pushDefault, branch remote, or the only configured remote",
        "Stops and asks the user to choose a publish remote when multiple remotes exist without a configured preference",
        "Uses git push -u <publish-remote> <current-branch> without asking for message or push approval",
        "Creates the PR after the push succeeds without a second review pause"
      ]
    },
    {
      "id": 7,
      "prompt": "/pr",
      "expected_behavior": "Pushes normal local-ahead upstream branches and creates the PR without a separate review gate.",
      "fixtures": [],
      "checks": [
        "Checks ahead and behind counts with git rev-list --left-right --count @{upstream}...HEAD",
        "Recognizes when the local branch is ahead of upstream",
        "Uses git push without asking for message or push approval",
        "Creates the PR after the push succeeds without a second review pause"
      ]
    },
    {
      "id": 8,
      "prompt": "/pr",
      "expected_behavior": "Stops before publishing when the upstream branch is ahead or diverged.",
      "fixtures": [],
      "checks": [
        "Checks ahead and behind counts with git rev-list --left-right --count @{upstream}...HEAD",
        "Detects remote-ahead state",
        "Detects diverged state",
        "Stops before pushing or creating a PR"
      ]
    },
    {
      "id": 9,
      "prompt": "create a PR for this branch",
      "expected_behavior": "Returns an existing PR URL instead of creating a duplicate PR.",
      "fixtures": [],
      "checks": [
        "Checks the selected GitHub repository with gh pr list filtered by both --head and --base",
        "Prints the existing PR URL when found",
        "Does not mistake an open PR to another base for a duplicate",
        "Does not push or create a duplicate PR"
      ]
    },
    {
      "id": 10,
      "prompt": "/pr",
      "expected_behavior": "Detects the GitHub provider, reports unavailable GitHub CLI authentication, and does not publish; it may still return a useful local draft.",
      "fixtures": [
        "draft-pr-format"
      ],
      "checks": [
        "Runs gh auth status",
        "Tells the user to authenticate with GitHub CLI if gh auth status fails",
        "Does not push or create a PR before authentication succeeds, and does not ask for content approval"
      ]
    },
    {
      "id": 11,
      "prompt": "Publish this Azure Repos branch as a pull request.",
      "expected_behavior": "Detects the Azure DevOps remote, selects the Azure CLI workflow, and never treats missing GitHub CLI authentication as the repository problem.",
      "fixtures": [
        "azure-devops-feature"
      ],
      "checks": [
        "Detects Azure DevOps from the dev.azure.com remote URL",
        "Derives widget-api from the remote and uses or proposes az repos show --repository widget-api",
        "Uses or proposes az repos commands rather than gh commands",
        "Does NOT run gh auth status, gh repo view, or gh pr create",
        "If Azure CLI prerequisites are unavailable, gives Azure-specific installation or authentication guidance",
        "Does NOT push or create a pull request when Azure CLI prerequisites are missing",
        "Does NOT ask the user to approve the generated title, body, or push command"
      ]
    },
    {
      "id": 12,
      "prompt": "Draft the PR title and description for this branch, but do not push or publish it. I ran `python3 -m unittest` and it passed.",
      "expected_behavior": "Returns a concise, evidence-based PR draft without requiring provider authentication or performing any network mutation.",
      "fixtures": [
        "draft-pr-format"
      ],
      "checks": [
        "Does NOT authenticate, push, or create a pull request",
        "Produces an imperative title under 70 characters with no trailing period",
        "Uses non-empty Summary and Validation sections with blank lines after the headings",
        "Keeps Summary to 1-3 outcome-focused bullets without a file inventory or title repetition",
        "Records `python3 -m unittest` as passed and does not invent other validation",
        "Omits empty Notes, placeholder text, and an approval request to publish"
      ]
    },
    {
      "id": 13,
      "prompt": "Draft the PR title and description against main, but do not authenticate, push, or publish.",
      "expected_behavior": "Uses the supplied local base to draft PR text even when the remote default symbolic ref is absent, without consulting a provider CLI.",
      "fixtures": [
        "draft-pr-without-remote-head"
      ],
      "checks": [
        "Uses main as the PR target and local comparison ref",
        "Does NOT run gh auth status, gh repo view, or any other provider lookup",
        "Does NOT push or create a pull request",
        "Returns a structured title and body based on the complete local diff"
      ]
    },
    {
      "id": 14,
      "prompt": "Draft the PR title and description for this branch, but do not push or publish it.",
      "expected_behavior": "Keeps the selected non-origin remote-tracking ref for local diff inspection while reporting the stripped branch name as the PR target.",
      "fixtures": [
        "non-origin-default-base"
      ],
      "checks": [
        "Selects upstream from branch.feat/friendly-greeting.pushRemote",
        "Reports main as the PR target branch",
        "Uses upstream/main rather than local main for git log and git diff",
        "Does NOT authenticate, push, or create a pull request"
      ]
    },
    {
      "id": 15,
      "prompt": "Draft the exact GitHub CLI commands that would publish this branch, but do not authenticate, push, or create the PR.",
      "expected_behavior": "Binds duplicate detection and PR creation commands to the repository derived from the selected non-origin remote and filters duplicates by both head and base.",
      "fixtures": [
        "non-origin-default-base"
      ],
      "checks": [
        "Derives github.com/example/widget-api from the selected upstream remote",
        "Uses gh pr list with --repo github.com/example/widget-api, --head feat/friendly-greeting, and --base main",
        "Uses gh pr create with --repo github.com/example/widget-api, --head feat/friendly-greeting, and --base main",
        "Does NOT authenticate, push, or create a pull request"
      ]
    }
  ]
}
