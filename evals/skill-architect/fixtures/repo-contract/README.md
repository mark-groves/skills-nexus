# Skills Repository

Place reusable cross-client skills under `skills/<name>/`. Every skill must
include `SKILL.md`, with its repository-only evaluation suite stored under
`evals/<name>/evals.json`. Follow the shape used by an existing skill and run
the repository's local validator after changes.

Harness integrations belong outside shared skill packages.
