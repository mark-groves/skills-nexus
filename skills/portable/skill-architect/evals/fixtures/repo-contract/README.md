# Portable Skills Repository

Place reusable cross-client skills under `skills/portable/<name>/`.
Every skill must include `SKILL.md` and `evals/evals.json`. Follow the
shape used by an existing portable skill and run the repository's local
validator after changes.

Harness integrations belong outside portable skill packages.
