# LLM Wiki

The core pattern has three layers: immutable raw sources, an LLM-maintained
markdown wiki, and a schema that tells the LLM how to maintain the wiki.

The wiki compounds over time because source summaries, entity pages,
concept pages, contradictions, and useful answers are filed back into the
markdown knowledge base instead of being re-derived from scratch.
