{
  "skill_name": "commit",
  "trigger_evals": [
    {
      "id": 1,
      "query": "commit my changes",
      "should_trigger": true
    },
    {
      "id": 2,
      "query": "make a conventional commit for the staged files",
      "should_trigger": true
    },
    {
      "id": 3,
      "query": "split these changes into two commits",
      "should_trigger": true
    },
    {
      "id": 4,
      "query": "open a pull request for my branch",
      "should_trigger": false
    },
    {
      "id": 5,
      "query": "show the git diff without committing anything",
      "should_trigger": false
    }
  ],
  "behavior_evals": [
    {
      "id": 1,
      "prompt": "/commit fix: typo in readme",
      "expected_behavior": "Fast-path commits a single obvious README change without conversational approval using a subject-only conventional commit message.",
      "fixtures": [
        "single-readme-fix-on-feature-branch"
      ],
      "checks": [
        "Does NOT ask the user for approval before committing",
        "Commit subject starts with 'fix:'",
        "Commit message has NO body paragraph",
        "Only README.md is included in the commit"
      ]
    },
    {
      "id": 2,
      "prompt": "commit my changes",
      "expected_behavior": "Commits only the intentionally staged files and mentions unstaged files as an FYI without staging them.",
      "fixtures": [
        "pre-staged-files-with-extra-unstaged-files"
      ],
      "checks": [
        "Only the 2 pre-staged files (src/auth.py and src/auth_test.py) are committed",
        "Unstaged files (config.yaml, docs/setup.md, src/utils.py) are NOT staged or committed",
        "Mentions the unstaged files as an FYI to the user",
        "Commit message uses a conventional commit prefix"
      ]
    },
    {
      "id": 3,
      "prompt": "split these changes into two commits",
      "expected_behavior": "Proposes multiple separate commits only when the unrelated changes map cleanly to separate files or already-staged hunks, and presents all groups before executing anything.",
      "fixtures": [
        "safe-multi-file-split"
      ],
      "checks": [
        "Proposes at least 2 separate commits for the unrelated changes",
        "Presents all proposed groups to the user BEFORE executing any commits",
        "Each proposed commit has an appropriate conventional commit prefix",
        "Does not bundle all changes into a single commit"
      ]
    },
    {
      "id": 4,
      "prompt": "commit these changes",
      "expected_behavior": "Stops immediately when the repo has unmerged paths or an in-progress sequencer operation instead of attempting a commit.",
      "fixtures": [
        "sequencer-merge-resolved"
      ],
      "checks": [
        "Detects unmerged/conflicted paths or an in-progress merge, rebase, cherry-pick, or revert",
        "Does NOT stage any files",
        "Does NOT create a commit",
        "Tells the user what must be resolved first"
      ]
    },
    {
      "id": 5,
      "prompt": "commit these changes",
      "expected_behavior": "Checks whether the current non-default branch matches the primary change and stops when the branch clearly points at unrelated work.",
      "fixtures": [],
      "checks": [
        "Evaluates whether the current branch name is related to the primary change being committed",
        "Does NOT assume any non-default branch is acceptable",
        "Warns when the branch clearly does not match the change",
        "Asks whether to create or switch to a more appropriate branch before committing"
      ]
    },
    {
      "id": 6,
      "prompt": "commit my changes",
      "expected_behavior": "When branch relevance is ambiguous, surfaces the ambiguity and asks the user instead of guessing.",
      "fixtures": [],
      "checks": [
        "Evaluates branch relevance before committing",
        "Treats an ambiguous branch-to-change mapping as a stop condition",
        "Asks the user whether to keep using the current branch or move the commit elsewhere",
        "Does NOT create a commit before the ambiguity is resolved"
      ]
    },
    {
      "id": 7,
      "prompt": "/commit",
      "expected_behavior": "Stops on detached HEAD and warns the user before any staging or commit.",
      "fixtures": [
        "detached-head"
      ],
      "checks": [
        "Detects detached HEAD from repo state",
        "Warns the user that committing on detached HEAD is unsafe",
        "Does NOT stage any files",
        "Does NOT create a commit"
      ]
    },
    {
      "id": 8,
      "prompt": "commit the staged changes",
      "expected_behavior": "When a file is partially staged, commits only the staged hunks and leaves unstaged hunks untouched.",
      "fixtures": [
        "partial-staging"
      ],
      "checks": [
        "Commits only the staged hunks from src/auth.py",
        "Does NOT restage src/auth.py",
        "Mentions that unstaged hunks remain in the working tree",
        "Commit message uses a conventional commit prefix"
      ]
    },
    {
      "id": 9,
      "prompt": "split these into two commits",
      "expected_behavior": "Does not auto-split unrelated unstaged hunks inside the same file; instead it tells the user to pre-stage or accept a single commit.",
      "fixtures": [
        "mixed-unstaged-hunks"
      ],
      "checks": [
        "Does NOT propose separate commits for unrelated unstaged hunks in the same file",
        "Does NOT use interactive staging such as git add -p",
        "Tells the user to pre-stage the intended hunks or accept a single commit",
        "Does NOT create a commit before the user resolves the ambiguity"
      ]
    },
    {
      "id": 10,
      "prompt": "commit my changes",
      "expected_behavior": "Gathers read-only git context with minimal tool chatter, checks sequencer sentinel paths in the same inspection pass, and keeps mutating steps separate.",
      "fixtures": [
        "sequencer-cherry-pick-resolved"
      ],
      "checks": [
        "Uses git status --short --branch for branch and worktree state",
        "Gathers read-only status, diffs, and recent log in a single Bash invocation when possible",
        "Checks git-dir sentinel paths for merge, cherry-pick, revert, and rebase state before mutating",
        "Uses git symbolic-ref --quiet --short HEAD when branch identity is needed",
        "Keeps staging, committing, and verification as separate mutating steps"
      ]
    }
  ]
}
